enum TasksListHierarchy {
  firstLevel,
  hierarchy,
  entireHierarchy,
}

enum FilterTypes {
  unknown,
  myRole,
  type,
  status,
  priority,
  deadline,
  files,
  tags,
  storypoints,
  performers
}

enum StillFilterTypes {
  files,
  tags,
}
