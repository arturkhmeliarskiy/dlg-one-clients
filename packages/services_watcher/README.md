# Services watcher

Services watcher package

## Installation 💻

Add `services_watcher` to your `pubspec.yaml`:

```yaml
dependencies:
  services_watcher:
```

Install it:

```sh
dart pub get
```
