# notifications_models

notifications_models package

## Installation 💻

Add `notifications_models` to your `pubspec.yaml`:

```yaml
dependencies:
  notifications_models:
```

Install it:

```sh
dart pub get
```
